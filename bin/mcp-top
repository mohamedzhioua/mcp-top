#!/usr/bin/env python3
"""Source-tree entrypoint for mcp-top."""

import pathlib
import sys


sys.path.insert(0, str(pathlib.Path(__file__).resolve().parents[1] / "src"))

from mcp_top.cli import main


raise SystemExit(main())
